# ── Material didático de terceiros ────────────────────────────────
# Slides do Prof. Dr. José Bezerra (BSBr) são propriedade dele.
# Ficam fora do repositório open-source; o método destilado em metodo/
# é autoral e cita a fonte.
material-fonte/

# ── Ferramentas de desenvolvimento ────────────────────────────────
# O BMad Method é a ferramenta usada para produzir este projeto, não
# parte dele. Quem quiser reproduzir o fluxo instala o BMad por conta
# própria — não redistribuímos código de terceiros aqui.
_bmad/
.claude/skills/bmad-*/
.claude/settings.local.json

# ── Python ────────────────────────────────────────────────────────
__pycache__/
*.py[cod]

# ── Sistema ───────────────────────────────────────────────────────
.DS_Store
Thumbs.db
